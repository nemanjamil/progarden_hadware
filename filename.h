#ifndef FILENAME_H
#define FILENAME_H
#include <Arduino.h> //needed for Serial.println
#include <string.h> //needed for memcpy

//int nekavarijabla = 7;

int foo(int a);
boolean isNegative(int x);  
int anotherOdity();
int udjiuconfValue();
void paligasi();

/*class GlavnaKlasa {
  public:
     //GlavnaKlasaParameter(const char *custom);
     //GlavnaKlasaParameter(const char *id, int length);
     const char *getID();
   
  private:
    //const char *_id;
    //void init(const char *id);
   
};
*/

  /*
  WiFiManager wifiManager; 
  wifiManager.resetSettings();
  if(!wifiManager.autoConnect("GardenLocal","password011")) {
    Serial.println("failed to connect and hit timeout");
    //reset and try again, or maybe put it to deep sleep
    ESP.reset();
    delay(1000);
  } 
  */

#endif 
